import { DismissableDirective } from './dismissable.directive';

describe('DismissableDirective', () => {
  it('should create an instance', () => {
    const directive = new DismissableDirective();
    expect(directive).toBeTruthy();
  });
});
