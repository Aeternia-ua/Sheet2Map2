import { SearchFiltersContainerDirective } from './search-filters-container.directive';

describe('AppSearchFiltersContainerDirective', () => {
  it('should create an instance', () => {
    const directive = new SearchFiltersContainerDirective();
    expect(directive).toBeTruthy();
  });
});
