import { InfoSidebarDirective } from './info-sidebar.directive';

describe('InfoSidebarDirective', () => {
  it('should create an instance', () => {
    const directive = new InfoSidebarDirective();
    expect(directive).toBeTruthy();
  });
});
